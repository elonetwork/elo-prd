apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "az-file.fullname" . }}-test-connection"
  labels:
    {{- include "az-file.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "az-file.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
